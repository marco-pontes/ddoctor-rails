<%= form_for(@usuario_perfil) do |f| %>
  <% if @usuario_perfil.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usuario_perfil.errors.count, "error") %> prohibited this usuario_perfil from being saved:</h2>

      <ul>
      <% @usuario_perfil.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :usuario %><br />
    <%= f.text_field :usuario %>
  </div>
  <div class="field">
    <%= f.label :perfil %><br />
    <%= f.text_field :perfil %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
